#include "main.h"

/**
 * _abs - print absolute value of parameter to console
 * @n: parameter to check
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);
	return (0);
}
